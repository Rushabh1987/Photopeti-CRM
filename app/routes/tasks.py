"""Editing-task routes — Phase 1/3."""
from fastapi import APIRouter

router = APIRouter(prefix="/api/editing-tasks", tags=["editing-tasks"])

# TODO(Phase 1): GET /
# TODO(Phase 1): PATCH /{id}  (set status, link shoot/client)

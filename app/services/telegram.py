"""Telegram delivery (Phase 4). Thin httpx wrapper over the Bot API sendMessage
endpoint; reads token + chat id from settings. No third-party SDK required."""
# TODO(Phase 4): send(text: str) -> bool

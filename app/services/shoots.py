"""Shoot service — create from a converted lead; flip status flags.
Set delivered_at when delivery_completed first turns true (the payment rule
keys off it). Implemented in Phase 1."""
# TODO(Phase 1)
